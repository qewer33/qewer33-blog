---
---

<header>
    <nav>
        <a href="https://qewer.dev/">Portfolio</a>
        <a href="/">Blogs</a>
    </nav>
</header>

<style>
header {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 999;
    background: linear-gradient(to bottom, rgba(var(--background-primary), 0.5) 60%, #0000);
}

header nav {
    width: 100%;
    height: 100px;
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    gap: 0px 25px;
}

header a {
    text-decoration: none;
    user-select: none;
    color: rgb(var(--text-primary));
    font-weight: bold;
    font-size: 0.8rem;
    padding: 10px 20px;
    text-transform: uppercase;
}

header a:hover {
    color: rgb(var(--accent-primary));
}
</style>